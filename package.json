{
	"name": "moq-js",
	"type": "module",
	"private": true,
	"workspaces": ["lib", "web"],
	"scripts": {
		"dev": "npm run dev --workspace web",
		"build": "npm run build --workspaces",
		"check": "biome check",
		"fix": "biome check --write"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4"
	}
}
